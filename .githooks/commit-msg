#!/bin/sh
pattern='^(feat|fix|chore|docs|refactor|test|perf|build|ci|style|revert)(\([a-z0-9-]+\))?!?: .+'
if ! head -1 "$1" | grep -Eq "$pattern"; then
  echo "commit message must be Conventional Commits, e.g. 'feat: add thing'" >&2
  exit 1
fi
